"use client";

import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  typography: {
    allVariants: {
      fontFamily: 'Averta Regular',
      textTransform: 'none',
      whiteSpace: 'nowrap',
      lineHeight: '24px',
    },
  },
});
